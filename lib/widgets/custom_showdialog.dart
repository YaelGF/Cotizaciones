import 'package:flutter/material.dart';

class CustomShowdialogWidget extends StatelessWidget {
  const CustomShowdialogWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}